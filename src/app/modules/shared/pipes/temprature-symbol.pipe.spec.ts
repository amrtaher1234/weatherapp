import { TempratureSymbolPipe } from './temprature-symbol.pipe';

describe('TempratureSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new TempratureSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
