import { TempratureValuePipe } from './temprature-value.pipe';

describe('TempratureValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TempratureValuePipe();
    expect(pipe).toBeTruthy();
  });
});
